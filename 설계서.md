# 설계서 — 건강기능식품 데일리 판매순위 대시보드

## 1. 인터뷰 요약 (2026-08-12)

| 항목 | 내용 |
|---|---|
| 목표 | 건강기능식품 데일리 판매순위를 한 페이지에서 확인 |
| 왜 필요한가 | 여러 사이트를 일일이 확인해야 해서 번거로움. 데일리 기록을 쌓아 월별 리포트도 만들고 싶음 |
| 접근 방향 | 특정 사이트에서 크롤링 |
| 결과물 형태 | 인터랙티브 대시보드 + 월간 리포트 둘 다 |
| 수집 항목 | 순위, 상품명, 가격 |
| 대상 사이트 | 카카오톡 선물하기, 다이소몰, 올리브영 |
| 열람 대상 | 우선 본인만 → 추후 팀/사내 공유로 확장 |
| 민감정보/보안 제약 | 없음 |
| 다룰 줄 아는 도구 | 엑셀 |
| 반복 주기 | 매일 자동 수집, 매월 초/말에 월간 집계 리포트 |
| 마감 | 급하지 않음, 천천히 진행 |
| 막막한 지점 | 없음 |

## 2. 추천 아키텍처

```
[사이트별 크롤러] → [일별 원시 데이터 저장(CSV/SQLite)] → [집계 로직] → [대시보드(HTML+JS)]
                                                                  └→ [월간 리포트(HTML, 월 1회 생성)]
```

### 2.1 크롤링
- Python + `requests`/`BeautifulSoup`(정적 페이지) 또는 `Playwright`(JS 렌더링이 필요한 페이지)
- 사이트마다 페이지 구조가 다르므로 사이트별로 수집 모듈을 분리
- 매일 1회 정해진 시간에 Windows 작업 스케줄러(또는 cron)로 자동 실행
- 유의사항: 사이트별 이용약관 확인, 과도한 요청으로 차단되지 않도록 요청 간 딜레이 필수,
  사이트 구조가 바뀌면 셀렉터가 깨질 수 있어 주기적 점검 필요

### 2.2 데이터 저장
- 1단계: 날짜별 CSV 또는 누적 CSV
- 필요해지면 SQLite로 전환 (쿼리·집계가 편해짐)
- 스키마: `날짜, 사이트, 순위, 상품명, 가격`

### 2.3 대시보드
- 정적 HTML + JS(Chart.js 등)로 시작 — 로컬 데이터 파일을 읽어 렌더링
- 필요 시 Flask/FastAPI로 서버화하여 실시간 갱신, 팀 공유 시 사내 서버나 GitHub Pages 배포
- UI/UX: 사이트별 탭 전환 + 순위 카드 그리드. `DESIGN-apple.md`의 카드/탭/pill 버튼
  컴포넌트를 그대로 적용

### 2.4 월간 리포트
- 월 1회(초/말) 배치 스크립트로 해당 월 데이터 집계(평균 순위, 순위 변동, 최다 1위 상품 등)
- self-contained HTML로 자동 생성, `DESIGN-apple.md` 스타일 적용

## 3. 로드맵

1. 인터뷰 & 방향 확정 — **완료**
2. 3개 사이트 크롤러 프로토타입 제작 — **완료** (아래 5번 참고)
3. 일별 자동 수집 — **완료** (아래 7번 참고)
4. 실제 데이터로 대시보드 연결 — **완료** (아래 7.2번 참고)
5. 월간 리포트 자동화
6. 팀 공유용으로 배포 (접근 권한/보안 재검토)

## 4. 개발 환경 (2026-08-12 설치 완료)

- Python 3.12 (winget으로 설치)
- 패키지: `requests`, `beautifulsoup4`, `playwright` (+ Chromium 브라우저 바이너리)
- 단순 `requests`/`WebFetch`로는 세 사이트 모두 접근 실패(올리브영 403 차단, 다이소몰·카카오는
  자바스크립트로 데이터를 나중에 불러오는 방식이라 빈 화면). **Playwright로 진짜 브라우저를
  띄워서 접근하니 세 사이트 모두 정상적으로 열리고 데이터도 보임.** 앞으로 크롤러는 전부
  Playwright 기반으로 작성한다.

## 5. 사이트별 조사 결과

### 카카오톡 선물하기 — ✅ 완료 (`scripts/crawl_kakao_gift.py`)
- `https://gift.kakao.com/ranking` → "건강" 탭 클릭 → "건강식품·영양제" 세부 카테고리 클릭
- 상품 카드: `div.unit_prd`, 각 카드 텍스트 안에 `브랜드명 : `, `상품명 : `, `판매가 : ` 라벨이
  그대로 있어 정규식으로 안정적으로 파싱 가능
- 매일 실행 시 상위 20개 정도 수집됨 (광고 포함 여부는 "광고" 배지로 구분)

### 올리브영 — ✅ 완료 (`scripts/crawl_oliveyoung.py`)
- 랭킹 페이지("건강식품" 탭)는 탭 요소가 화면상 보이지 않는 상태라 클릭이 막혀서, 탭의
  JS 핸들러(`common.link.moveCategoryShop('10000020001', 'Drawer', {})`)를 직접 호출해
  카테고리 페이지로 이동: `https://www.oliveyoung.co.kr/store/display/getCategoryShop.do?dispCatNo=10000020001&gateCd=Drawer&`
- 그 페이지 안의 "건강식품의 BEST만 모아봤어요" 위젯이 곧 카테고리별 판매 랭킹.
  선택자: `div.ct-product#mRnkGoodsSec > div.item`, 항목 안에서
  `span.num`(순위) · `span.prd-name`(상품명) · `p.price span.price-2`(가격) ·
  `button.btn_zzim[data-ref-goodsbrand]`(브랜드)
- 이 위젯은 **상위 5개만** 노출됨 (사이트 자체 설계상 제한, "더보기"로 더 볼 수 있으나
  프로토타입에서는 5개까지만 수집)

### 다이소몰 — ✅ 완료 (`scripts/crawl_daiso.py`)
- `https://www.daisomall.co.kr/ds/rank/new` (랭킹 페이지)에 건강기능식품 전용 필터는 없고,
  카테고리 버튼 중 "식품"이 가장 가까운 상위 카테고리 (버튼 클릭으로 필터링됨,
  `page.get_by_role("button", name="식품").click()`)
- "식품" 랭킹은 실제로는 상위권 대부분이 홍삼·유산균·비타민 필름형 제품이라 건강기능식품
  트래킹 목적에 충분히 근접함 (예: 동국제약 필름류, 휴럼 야채분말 등)
- DOM 선택자 대신, 페이지 전체 텍스트에서 `"현재 순위\n(숫자)\n...장바구니\n담기\n...([\d,]+)\n원\n(상품명)"`
  패턴을 정규식으로 파싱 (개별 카드 DOM 구조가 xpath로 안정적으로 잡히지 않아 텍스트 파싱이
  더 안정적)
- 상품별 브랜드명은 상품명 문자열 안에 섞여 있어 별도 필드로 분리하지 않음 (필요해지면
  상품명 앞부분 대괄호/브랜드 패턴으로 추출 가능)

## 7. 자동화 (GitHub Actions)

- 저장소: https://github.com/cptmqhdl/healthrank (비공개)
- `.github/workflows/daily-crawl.yml`이 매일 `cron: "30 0 * * *"`(UTC, = 09:30 KST)에
  자동 실행됨. 한국은 서머타임이 없어 연중 동일 시각.
- **내 PC 전원 상태와 무관** — 깃허브의 클라우드 서버에서 실행되므로 PC가 꺼져 있어도 정상
  작동함
- 실행 순서: Python 3.12 설치 → Playwright + Chromium 설치 → 크롤러 3개 순서대로 실행
  (하나가 실패해도 나머지는 계속 진행) → 결과 CSV를 `data/raw/`에 커밋 & 푸시
- 크롤러가 상품을 하나도 못 찾으면 `SystemExit(1)`로 실패 처리되어, Actions 탭에서 빨간
  표시로 바로 확인 가능 (사이트 구조가 바뀐 신호)
- 수동 실행: 저장소 Actions 탭 → 워크플로우 선택 → "Run workflow" 버튼 (매일 기다리지
  않고 바로 테스트 가능)
- 로컬 PC의 이 폴더는 이제 "보기 편한 사본" 역할 — 실제 매일 쌓이는 데이터는 깃허브
  저장소에 쌓임. 최신 데이터를 로컬에서 보려면 동기화(pull)가 필요 (4단계에서 대시보드를
  깃허브 페이지로 옮기면 이 문제 자체가 없어짐)

## 7.1 깃허브 페이지 배포 (2026-08-12)

- 대시보드를 바로 웹으로 테스트해보고 싶다는 요청으로, 저장소를 **비공개 → 공개**로 전환하고
  깃허브 페이지(GitHub Pages)를 켬 (무료 개인 계정은 비공개 저장소에서 Pages 기능을 쓸 수
  없어 공개 전환이 필요했음; 판매순위 데이터는 민감정보가 아니라 공개해도 무방하다고 판단)
- 주소: https://cptmqhdl.github.io/healthrank/
- 루트 접속 시 `report.html`로 자동 이동하도록 `index.html`(0초 리다이렉트) 추가
- 현재는 `report.html`이 샘플 데이터 상태라, 이 주소도 아직 샘플 화면만 보여줌. 4단계
  완료 후 실제 데이터가 이 주소에 그대로 반영됨
- 6단계(팀 공유)에서 공개 여부를 다시 검토할 것 — 지금은 "즉시 테스트" 목적의 임시 조치

## 7.2 대시보드-데이터 연결 (2026-08-12, 4단계)

- 방식: 정적 사이트 + 클라이언트 JS `fetch` (별도 백엔드 서버 없음)
- `scripts/build_dashboard_data.py`:
  - `data/raw/{사이트}_YYYY-MM-DD.csv` 중 사이트별 **가장 최근 날짜 파일**을 읽어 `data/latest.json`
    으로 가공. CSV 파싱은 Python 표준 `csv` 모듈 사용(상품명에 쉼표가 섞여 있어 카카오 데이터는
    실제로 따옴표로 감싸진 필드가 존재함 — 단순 `split(',')`으로는 깨짐)
  - **화면에는 사이트별 1~10위까지만 노출** (`TOP_N = 10`). 크롤러 자체는 더 많이 수집해도
    (카카오 20개, 다이소 27개) 표시는 10개로 제한. 올리브영은 사이트 위젯 구조상 애초에
    5개까지만 수집되므로 5개만 보임
  - 전날 파일이 있으면 **상품명을 키로 매칭**해 순위 변동(▲상승/▼하락/–동일/NEW)을 계산.
    전날 파일이 없으면(수집 첫날) 전부 NEW로 표시
  - 이 스크립트는 `.github/workflows/daily-crawl.yml`의 크롤러 실행 스텝 다음에 자동 실행되고,
    `data/latest.json`도 `data/raw`와 함께 커밋됨 — 사람이 손댈 일 없이 매일 갱신
- `report.html`: 페이지 로드 시 `fetch('data/latest.json')`으로 불러와 탭(사이트별)과 순위
  카드를 JS로 동적 생성. **로컬에서 파일을 더블클릭해 열면 브라우저의 `file://` 보안 정책
  때문에 fetch가 막혀 데이터가 안 보일 수 있음** — 반드시 웹 서버(깃허브 페이지 등)를 통해
  열어야 함. fetch 실패 시 화면에 안내 문구를 보여주도록 처리함
- 상품명은 `escapeHtml`로 이스케이프해 XSS 위험 없이 innerHTML로 렌더링

## 7.3 상품 썸네일·클릭 링크 (2026-08-12, 4단계 보완)

- 문제: 크롤러 3개는 이미 상품 상세 링크(`링크`)와 썸네일 이미지 주소(`이미지`)를 CSV에
  수집하고 있었지만, `build_dashboard_data.py`가 이 두 컬럼을 `latest.json`으로 넘기지
  않고 `report.html`도 이를 화면에 그리지 않아 실제로는 안 보이는 상태였음 (직전 세션에서
  크롤러만 고치고 데이터 가공/화면 단은 마무리하지 못한 채 종료됨)
- 조치:
  - `build_dashboard_data.py`: 각 상품 항목에 `link`, `image` 필드 추가
  - `report.html`: 순위 카드 상단에 정사각형 썸네일(`.rank-thumb`, `background-image`로
    표시, 이미지가 없으면 빈 칸) 추가. 카드 전체를 `<a href="상품링크" target="_blank"
    rel="noopener noreferrer">`로 감싸 클릭 시 새 탭에서 실제 판매 페이지로 이동
    (링크가 없는 경우에만 기존처럼 `<div>`로 렌더링)
  - 다이소몰·올리브영은 오늘자(2026-08-12) CSV가 링크/이미지 컬럼이 추가되기 전 데이터라
    재수집 필요 → 두 크롤러를 다시 실행해 갱신
- 확인: `data/latest.json`을 직접 열어 세 사이트 전 상품이 `link`/`image` 값을 갖고 있는지,
  사이트별 노출 개수(카카오 10 · 다이소 10 · 올리브영 5)가 맞는지 로컬 웹서버
  (`python -m http.server`)로 확인 완료

## 8. 열린 질문 / 리스크

- 사이트 구조가 바뀌면 유지보수 필요 — 선택자/카테고리 코드가 깨질 수 있어 주기적 점검 필요
- 자동화가 매일 도니, 사이트에서 접속을 차단할 가능성을 계속 지켜봐야 함 (막히면 크롤러가
  SystemExit(1)로 실패해서 Actions 탭에 표시됨)
- 팀 공유 단계에서는 저장소 공개 여부/접근 권한을 다시 검토 (지금은 비공개)
- 자동 접속이 반복되면 사이트에서 차단할 가능성이 있으므로, 매일 실행 간격을 적당히 두고
  과도한 요청은 피할 것

---
*이 문서는 진행 상황에 맞춰 계속 업데이트합니다.*
