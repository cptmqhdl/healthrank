# 설계서 — 건강기능식품 데일리 판매순위 대시보드

## 1. 인터뷰 요약 (2026-08-12)

| 항목 | 내용 |
|---|---|
| 목표 | 건강기능식품 데일리 판매순위를 한 페이지에서 확인 |
| 왜 필요한가 | 여러 사이트를 일일이 확인해야 해서 번거로움. 데일리 기록을 쌓아 월별 리포트도 만들고 싶음 |
| 접근 방향 | 특정 사이트에서 크롤링 |
| 결과물 형태 | 인터랙티브 대시보드 + 월간 리포트 둘 다 |
| 수집 항목 | 순위, 상품명, 가격 |
| 대상 사이트 | 카카오톡 선물하기, 다이소몰, 올리브영 |
| 열람 대상 | 우선 본인만 → 추후 팀/사내 공유로 확장 |
| 민감정보/보안 제약 | 없음 |
| 다룰 줄 아는 도구 | 엑셀 |
| 반복 주기 | 매일 자동 수집, 매월 초/말에 월간 집계 리포트 |
| 마감 | 급하지 않음, 천천히 진행 |
| 막막한 지점 | 없음 |

## 2. 추천 아키텍처

```
[사이트별 크롤러] → [일별 원시 데이터 저장(CSV/SQLite)] → [집계 로직] → [대시보드(HTML+JS)]
                                                                  └→ [월간 리포트(HTML, 월 1회 생성)]
```

### 2.1 크롤링
- Python + `requests`/`BeautifulSoup`(정적 페이지) 또는 `Playwright`(JS 렌더링이 필요한 페이지)
- 사이트마다 페이지 구조가 다르므로 사이트별로 수집 모듈을 분리
- 매일 1회 정해진 시간에 Windows 작업 스케줄러(또는 cron)로 자동 실행
- 유의사항: 사이트별 이용약관 확인, 과도한 요청으로 차단되지 않도록 요청 간 딜레이 필수,
  사이트 구조가 바뀌면 셀렉터가 깨질 수 있어 주기적 점검 필요

### 2.2 데이터 저장
- 1단계: 날짜별 CSV 또는 누적 CSV
- 필요해지면 SQLite로 전환 (쿼리·집계가 편해짐)
- 스키마: `날짜, 사이트, 순위, 상품명, 가격`

### 2.3 대시보드
- 정적 HTML + JS(Chart.js 등)로 시작 — 로컬 데이터 파일을 읽어 렌더링
- 필요 시 Flask/FastAPI로 서버화하여 실시간 갱신, 팀 공유 시 사내 서버나 GitHub Pages 배포
- UI/UX: 사이트별 탭 전환 + 순위 카드 그리드. `DESIGN-apple.md`의 카드/탭/pill 버튼
  컴포넌트를 그대로 적용

### 2.4 월간 리포트
- 월 1회(초/말) 배치 스크립트로 해당 월 데이터 집계(평균 순위, 순위 변동, 최다 1위 상품 등)
- self-contained HTML로 자동 생성, `DESIGN-apple.md` 스타일 적용

## 3. 로드맵

1. 인터뷰 & 방향 확정 — **완료**
2. 3개 사이트 크롤러 프로토타입 제작 — **완료** (아래 5번 참고)
3. 일별 자동 수집 — **완료** (아래 7번 참고)
4. 실제 데이터로 대시보드 연결 — **완료** (아래 7.2번 참고)
5. 월간 리포트 자동화 — **완료** (아래 7.14번 참고)

> 원래 6단계로 "팀/사내 공유용으로 배포"가 있었으나 로드맵에서 제외함 — 아래 7.18절 참고.
> 지금은 5단계까지 완료된 상태를 이 프로젝트의 목표 범위로 본다.

## 4. 개발 환경 (2026-08-12 설치 완료)

- Python 3.12 (winget으로 설치)
- 패키지: `requests`, `beautifulsoup4`, `playwright` (+ Chromium 브라우저 바이너리)
- 단순 `requests`/`WebFetch`로는 세 사이트 모두 접근 실패(올리브영 403 차단, 다이소몰·카카오는
  자바스크립트로 데이터를 나중에 불러오는 방식이라 빈 화면). **Playwright로 진짜 브라우저를
  띄워서 접근하니 세 사이트 모두 정상적으로 열리고 데이터도 보임.** 앞으로 크롤러는 전부
  Playwright 기반으로 작성한다.

## 5. 사이트별 조사 결과

### 카카오톡 선물하기 — ✅ 완료, 2026-08-12 링크 직결로 재정비 (`scripts/crawl_kakao_gift.py`)
- 처음엔 `https://gift.kakao.com/ranking`에서 "건강" 탭 → "건강식품·영양제" 세부 카테고리를
  순서대로 클릭해 들어갔으나, 사용자가 직접 카테고리 랭킹 페이지 링크
  (`https://gift.kakao.com/ranking/category/8`)를 제공해줘서 클릭 2단계 없이 바로 이 URL로
  이동하도록 단순화 (더 안정적)
- 상품 카드: `div.unit_prd`, 각 카드 텍스트 안에 `브랜드명 : `, `상품명 : `, `판매가 : ` 라벨이
  그대로 있어 정규식으로 안정적으로 파싱 가능. 상품 링크(`a.link_prdunit`)와 썸네일
  이미지(`img.img_thumb`)도 함께 수집
- **주의**: 이 페이지는 "광고(스폰서)" 상품 5개가 별도로 자체 1~5위 번호를 달고 먼저 나온
  뒤에 진짜 판매순위 1~20위가 이어지는 구조라, 광고 상품을 그대로 두면 진짜 1~5위와
  번호가 겹쳐버림. 그래서 광고 상품(카드 상단 "광고" 배지)은 판매순위 집계에서 아예
  제외하고, 광고를 뺀 나머지만 순서대로 수집 (20개 정도)

### 올리브영 — ✅ 완료, 2026-08-12 XHR 직접 호출 방식으로 재정비 (`scripts/crawl_oliveyoung.py`)
- 기존에는 카테고리 페이지의 "건강식품의 BEST만 모아봤어요" 위젯을 읽었는데 이 위젯이
  **상위 5개까지만** 노출되는 구조적 한계가 있었음
- 사용자가 올리브영 랭킹 페이지의 "인기상품 더보기" 버튼이 실제로 호출하는 주소
  (`getBestList.do?dispCatNo=900000100100001&fltDispCatNo=10000020001&...`)를 제공해줘서,
  이 주소로 바꿈 — 단, 이 주소를 브라우저 주소창처럼 그냥 이동(GET 네비게이션)하면
  올리브영이 "랭킹" 전체 페이지 껍데기만 돌려줌. 실제 상품 목록을 받으려면 올리브영
  사이트를 한 번 방문해 세션 쿠키를 확보한 뒤, 그 페이지 안에서 `fetch()`로
  `X-Requested-With: XMLHttpRequest` 헤더를 포함해 같은 주소를 호출해야 함(AJAX 방식
  요청이라고 서버에 알려주는 것) — 그러면 `ul.cate_prd_list > li` 안에 순위 1~100위가
  전부 담긴 HTML이 내려옴
- 선택자: `.thumb_flag.best`(순위 "01","02"…) · `.tx_brand`(브랜드) · `.tx_name`(상품명) ·
  `.prd_price .tx_cur .tx_num`(할인 적용된 실제 판매가) · `a.prd_thumb`(링크) ·
  `a.prd_thumb img`(썸네일)
- 결과적으로 **1~10위 전부** 안정적으로 수집 가능해짐 (예전의 "5개만" 제한이 사라짐)

### 다이소몰 — ✅ 완료, 2026-08-12 전용 랭킹 위젯으로 재정비 (`scripts/crawl_daiso.py`)
- 기존에는 건강기능식품 전용 필터가 없는 일반 "식품" 랭킹 페이지(`/ds/rank/new`)를 근사치로
  사용했는데, 사용자가 실제 "건강식품" 카테고리 페이지(`https://www.daisomall.co.kr/ds/diy2/C246`)
  링크를 제공해줘서 이 페이지 안의 **"건강식품 카테고리별 랭킹"** 위젯(진짜 건강기능식품
  전용 랭킹)으로 교체
- 이 위젯은 스와이프 캐러셀(swiper)이라 카드 하나씩 순서대로 접근하면 화면이 움직이며
  일부 카드에서 타임아웃이 발생함 → 카드 전체를 JS `evaluate()` 한 번으로 스냅샷 떠서
  가져오는 방식으로 해결
- 선택자: `div.nav-rank div.product-card` 각 카드 안에서 `.rank .num`(순위) ·
  `.product-title`(상품명) · `.price-value .value`(가격) · `a.prod-thumb__link`(링크) ·
  `.prod-thumb img`(썸네일, 일부 상품은 `thumb-img-default` 클래스가 없고 `thumb-img`
  단일 이미지만 있어 클래스명 대신 `.prod-thumb img`로 더 넓게 선택)
- 위젯 자체가 1~24위까지 제공하므로 그중 1~10위만 필터링해서 사용

## 7. 자동화 (GitHub Actions)

- 저장소: https://github.com/cptmqhdl/healthrank (비공개)
- `.github/workflows/daily-crawl.yml`이 매일 `cron: "30 0 * * *"`(UTC, = 09:30 KST)에
  자동 실행됨. 한국은 서머타임이 없어 연중 동일 시각.
- **내 PC 전원 상태와 무관** — 깃허브의 클라우드 서버에서 실행되므로 PC가 꺼져 있어도 정상
  작동함
- 실행 순서: Python 3.12 설치 → Playwright + Chromium 설치 → 크롤러 3개 순서대로 실행
  (하나가 실패해도 나머지는 계속 진행) → 결과 CSV를 `data/raw/`에 커밋 & 푸시
- 크롤러가 상품을 하나도 못 찾으면 `SystemExit(1)`로 실패 처리되어, Actions 탭에서 빨간
  표시로 바로 확인 가능 (사이트 구조가 바뀐 신호)
- 수동 실행: 저장소 Actions 탭 → 워크플로우 선택 → "Run workflow" 버튼 (매일 기다리지
  않고 바로 테스트 가능)
- 로컬 PC의 이 폴더는 이제 "보기 편한 사본" 역할 — 실제 매일 쌓이는 데이터는 깃허브
  저장소에 쌓임. 최신 데이터를 로컬에서 보려면 동기화(pull)가 필요 (4단계에서 대시보드를
  깃허브 페이지로 옮기면 이 문제 자체가 없어짐)

## 7.1 깃허브 페이지 배포 (2026-08-12)

- 대시보드를 바로 웹으로 테스트해보고 싶다는 요청으로, 저장소를 **비공개 → 공개**로 전환하고
  깃허브 페이지(GitHub Pages)를 켬 (무료 개인 계정은 비공개 저장소에서 Pages 기능을 쓸 수
  없어 공개 전환이 필요했음; 판매순위 데이터는 민감정보가 아니라 공개해도 무방하다고 판단)
- 주소: https://cptmqhdl.github.io/healthrank/
- 루트 접속 시 `report.html`로 자동 이동하도록 `index.html`(0초 리다이렉트) 추가
- 현재는 `report.html`이 샘플 데이터 상태라, 이 주소도 아직 샘플 화면만 보여줌. 4단계
  완료 후 실제 데이터가 이 주소에 그대로 반영됨
- (2026-08-12 업데이트) 원래 "6단계(팀 공유)에서 공개 여부를 다시 검토"할 계획이었으나
  6단계 자체가 로드맵에서 제외됨(7.18절 참고) — 당분간은 공개 저장소 상태를 그대로
  유지. 판매순위 데이터가 민감정보는 아니라서 문제는 없음

## 7.2 대시보드-데이터 연결 (2026-08-12, 4단계)

- 방식: 정적 사이트 + 클라이언트 JS `fetch` (별도 백엔드 서버 없음)
- `scripts/build_dashboard_data.py`:
  - `data/raw/{사이트}_YYYY-MM-DD.csv` 중 사이트별 **가장 최근 날짜 파일**을 읽어 `data/latest.json`
    으로 가공. CSV 파싱은 Python 표준 `csv` 모듈 사용(상품명에 쉼표가 섞여 있어 카카오 데이터는
    실제로 따옴표로 감싸진 필드가 존재함 — 단순 `split(',')`으로는 깨짐)
  - **화면에는 사이트별 1~10위까지만 노출** (`TOP_N = 10`). 세 사이트 모두 크롤러 단계에서
    이미 1~10위(또는 그 이상)를 수집하므로 표시는 항상 10개로 맞춰짐 (2026-08-12 크롤러
    재정비 이후 — 자세한 내용은 5절 참고)
  - 전날 파일이 있으면 **상품명을 키로 매칭**해 순위 변동(▲상승/▼하락/–동일/NEW)을 계산.
    전날 파일이 없으면(수집 첫날) 전부 NEW로 표시
  - 이 스크립트는 `.github/workflows/daily-crawl.yml`의 크롤러 실행 스텝 다음에 자동 실행되고,
    `data/latest.json`도 `data/raw`와 함께 커밋됨 — 사람이 손댈 일 없이 매일 갱신
- `report.html`: 페이지 로드 시 `fetch('data/latest.json')`으로 불러와 탭(사이트별)과 순위
  카드를 JS로 동적 생성. **로컬에서 파일을 더블클릭해 열면 브라우저의 `file://` 보안 정책
  때문에 fetch가 막혀 데이터가 안 보일 수 있음** — 반드시 웹 서버(깃허브 페이지 등)를 통해
  열어야 함. fetch 실패 시 화면에 안내 문구를 보여주도록 처리함
- 상품명은 `escapeHtml`로 이스케이프해 XSS 위험 없이 innerHTML로 렌더링

## 7.3 상품 썸네일·클릭 링크 (2026-08-12, 4단계 보완)

- 문제: 크롤러 3개는 이미 상품 상세 링크(`링크`)와 썸네일 이미지 주소(`이미지`)를 CSV에
  수집하고 있었지만, `build_dashboard_data.py`가 이 두 컬럼을 `latest.json`으로 넘기지
  않고 `report.html`도 이를 화면에 그리지 않아 실제로는 안 보이는 상태였음 (직전 세션에서
  크롤러만 고치고 데이터 가공/화면 단은 마무리하지 못한 채 종료됨)
- 조치:
  - `build_dashboard_data.py`: 각 상품 항목에 `link`, `image` 필드 추가
  - `report.html`: 순위 카드 상단에 정사각형 썸네일(`.rank-thumb`, `background-image`로
    표시, 이미지가 없으면 빈 칸) 추가. 카드 전체를 `<a href="상품링크" target="_blank"
    rel="noopener noreferrer">`로 감싸 클릭 시 새 탭에서 실제 판매 페이지로 이동
    (링크가 없는 경우에만 기존처럼 `<div>`로 렌더링)
  - 다이소몰·올리브영은 오늘자(2026-08-12) CSV가 링크/이미지 컬럼이 추가되기 전 데이터라
    재수집 필요 → 두 크롤러를 다시 실행해 갱신
- 확인: `data/latest.json`을 직접 열어 세 사이트 전 상품이 `link`/`image` 값을 갖고 있는지,
  사이트별 노출 개수(카카오 10 · 다이소 10 · 올리브영 5)가 맞는지 로컬 웹서버
  (`python -m http.server`)로 확인 완료

## 7.4 화면 정리 — 판매순위 전용 대시보드로 단순화 (2026-08-12)

- 사용자 요청: "이 대시보드에 제품 판매순위만 남겨놓고 싶다"는 방향에 따라 인터뷰/추천/로드맵
  성격의 섹션은 목적에 안 맞는다고 판단해 삭제
- `report.html`에서 제거한 섹션: "이렇게 만들 것을 추천해요"(기술스택·툴·UI 추천 카드),
  "다음 단계 로드맵"(1~6단계 진행 표시), "지금 할 수 있는 다음 액션 3가지"(액션 카드 +
  다음 세션 이어가기용 프롬프트 복사 박스). 관련 CSS(`.rec-grid`, `.roadmap`, `.actions`,
  `.prompt-box`, `.copy-btn` 등)와 JS(복사 버튼 이벤트)도 함께 정리
- 상단 히어로(목표·결과물 요약 카드)와 "오늘의 판매순위" 섹션, 푸터는 유지 — 필요하면
  다음에 히어로도 더 줄일 수 있음(사용자 확인 필요)
- 진행 상황 확인용 로드맵 문서 역할은 이제 이 `설계서.md`와 `README.md`가 전담

## 7.5 판매순위 섹션 라이트 모드 전환 (2026-08-12)

- 문제: "오늘의 판매순위" 섹션이 `tile-dark`(항상 검정 배경 고정) 스타일이라, 애플 iOS
  라이트 모드 느낌과 맞지 않는다는 피드백
- 조치: 섹션을 `tile-dark` → `tile-alt`(연회색 배경, 라이트/다크 모드에 자동 대응하는
  시맨틱 토큰)로 전환. 탭 버튼·순위 카드·썸네일 배경 등 하위 컴포넌트도 고정된
  `--tile-dark-*` 토큰 대신 `--surface`/`--text`/`--surface-border` 같은 전역 시맨틱
  토큰을 쓰도록 변경 — 결과적으로 흰 카드 + 연회색 배경의 iOS 그룹 리스트 느낌이 되었고,
  다크 모드 기기에서도(별도 분기 없이) 자동으로 다크 팔레트를 따라감
  (`DESIGN-apple.md` 원칙 유지)
- 정리: 더 이상 쓰이지 않게 된 `--tile-dark-*` CSS 변수와 `.tile-dark` 클래스 규칙 삭제

## 7.6 탭 제거 → 채널별 구획 레이아웃 (2026-08-12)

- 사용자 피드백: "탭이 기능을 안 해" — 탭 전환 UI가 실제로는 잘 동작하지 않는다는 문제
  제기와 함께, 채널(카카오/다이소/올리브영)별로 더 직관적으로 나눠 보여달라는 요청
- 조치: 탭(`.tabs`, `.tab-btn`, 클릭 시 전환하는 JS)을 완전히 제거하고, 세 사이트를 위에서
  아래로 순서대로 쌓아 보여주는 구조로 변경. 사이트별로 `.channel-block` 하나씩 — 굵은
  구분선 아래 사이트명(`.channel-name`)과 "날짜 기준 · N개"(`.channel-meta`)를 표시한 뒤
  그 아래 기존 순위 카드 그리드(`.rank-grid`)를 그대로 이어붙임
  (`renderSite()` 함수를 탭+패널 생성 방식에서 섹션 생성 방식으로 재작성)
- 스크롤만으로 세 채널을 모두 볼 수 있어 클릭 상호작용에 의존하지 않음 — 탭이 안 먹는
  문제 자체가 원천적으로 사라짐

## 7.7 세 사이트 크롤러 전면 재정비 (2026-08-12)

- 배경: 채널별 구획 레이아웃(7.6)으로 바꾼 뒤, 사용자가 세 사이트의 정확한 랭킹 페이지
  링크를 직접 찾아서 하나씩 제공해줌 — 카카오
  (`https://gift.kakao.com/ranking/category/8`), 다이소
  (`https://www.daisomall.co.kr/ds/diy2/C246`의 "건강식품 카테고리별 랭킹" 위젯), 올리브영
  (`getBestList.do?...fltDispCatNo=10000020001...`, 인기상품 더보기가 호출하는 주소)
- 세 크롤러 모두 이 링크들을 반영해 재작성함 (각 사이트별 구체적인 변경 내용은 5절 참고).
  핵심 성과: **올리브영이 5개 → 10개로 확장**되어 이제 세 사이트 모두 예외 없이 1~10위
  전부 채워짐. 카카오는 광고 상품 혼입 문제를 해결. 다이소는 "식품" 근사 카테고리 대신
  진짜 "건강식품" 전용 랭킹으로 교체
- `data/latest.json` 재생성 결과 3개 사이트 모두 `count: 10`, 전 상품 `link`/`image` 값
  보유 확인 완료 (로컬 웹서버로 재검증)

## 7.8 카카오 썸네일 6~10위 미표시 버그 수정 (2026-08-12)

- 문제: 사용자가 실제 화면에서 확인해보니 카카오톡 선물하기 6~10위 상품의 썸네일이
  안 보임
- 원인: 카카오 랭킹 페이지의 상품 이미지가 **lazy loading**(`uselazyloading` 속성)으로
  구현돼 있어, 카드가 화면(뷰포트)에 스크롤로 노출되기 전까지는 `img.img_thumb`의
  `src`가 기본 placeholder(`default_fallback_thumbnail.png`)로 남아있음. 처음 페이지를
  열었을 때 화면에 보이는 카드는 4~5개 정도뿐이라, 그 아래 순위(5~10위 이하)는 placeholder
  이미지 그대로 수집되고 있었음
- 조치: 상품 정보를 읽기 전에 모든 카드를 한 번씩 `scroll_into_view_if_needed()`로
  스크롤해 lazy loading을 미리 전부 트리거한 뒤 이미지 주소를 읽도록 수정
  (`scripts/crawl_kakao_gift.py`)
- 확인: 재수집 후 1~10위 전부 `default_fallback_thumbnail.png`가 아닌 실제 상품
  이미지 주소를 갖고 있음을 확인

## 7.9 채널별 타이틀에 브랜드 키컬러 적용 (2026-08-12)

- 요청: 채널 구획(7.6)만으로는 구분이 약하다는 판단하에, 각 채널 타이틀 영역 배경에
  해당 사이트의 브랜드 키컬러를 적용해 구분성·가독성을 높여달라는 요청
- 적용한 색상: 카카오톡 선물하기 `#FEE500`(카카오 옐로우, 텍스트는 카카오가 실제
  쓰는 진한 브라운 `#381E1E`) · 다이소몰 `#ED1C24`(다이소 레드, 흰 텍스트) · 올리브영
  `#9BCE26`(올리브영 공식 사이트 CSS에서 그대로 확인한 시그니처 그린, 짙은 텍스트)
- `report.html`의 `.channel-head`에 `channel-head--kakao` / `channel-head--daiso` /
  `channel-head--oliveyoung` 수정자 클래스를 사이트 id 기준으로 붙여 배경색을 적용.
  타이틀 영역이 카드형 pill 배경이 되도록 `border-radius:14px` 적용, 부제(날짜·개수)는
  `color:inherit; opacity:0.72`로 처리해 배경색이 바뀌어도 항상 읽히게 함
- 이 부분은 `DESIGN-apple.md`의 "단일 액션 블루" 원칙에서 의도적으로 벗어난 예외 —
  채널 구분이라는 명확한 목적이 있어 사용자가 직접 요청한 사항

## 7.10 올리브영 "1~5위만 보임" 문의 — 실제로는 CDN 캐시 지연 (2026-08-12)

- 문의: 사용자가 화면에서 올리브영이 1~5위까지만 보인다고 알려줌
- 확인: 깃허브 저장소의 `data/latest.json`과 배포된 `https://cptmqhdl.github.io/healthrank/`
  양쪽 다 직접 조회해보니 이미 올리브영 `count: 10`, 1~10위 전부 정상 존재 — 즉 데이터/코드
  문제가 아니었음
- 원인: 깃허브 페이지는 Fastly CDN을 통해 서비스되는데 `report.html`·`data/latest.json`
  둘 다 `Cache-Control: max-age=600`(10분) 헤더가 붙어있음. 직전 커밋을 올린 지 10분이
  안 된 시점에 사용자가 화면을 열어봐서, CDN 엣지에 아직 남아있던 이전 버전(5개였던
  시절)이 그대로 보였을 가능성이 높음 — `fetch()`에 준 `cache:'no-store'`는 브라우저
  로컬 캐시만 우회할 뿐, CDN 엣지 캐시까지는 막지 못함
- 조치: 코드 변경 없음. 몇 분 뒤 새로고침(또는 강력 새로고침)하면 해결됨을 안내

## 7.11 다이소 8~10위 썸네일 미표시 버그 수정 (2026-08-12)

- 문제: 사용자가 실제 화면에서 다이소몰 8~10위 상품의 썸네일이 안 보인다고 확인해줌
- 원인: 다이소 랭킹 위젯은 Swiper 캐러셀의 **`swiper-lazy`** 지연 로딩을 사용함. 화면에
  아직 노출(활성화)되지 않은 슬라이드는 `<img>`의 `src`가 placeholder
  (`/images/tempLy/noimage.png`)로 남아있고, 실제 이미지 주소는 `data-src` 속성에 미리
  들어있음. 기존 코드는 `src`만 읽어서, placeholder를 값으로 착각해 "이미지가 있다"고
  판단(빈 문자열이 아니므로)해버렸음 — 정작 그 placeholder 자체가 잘못된 이미지였음
- 조치: 이미지 주소를 읽을 때 `data-src`를 우선 사용하고, 없으면 `src`로 대체하도록
  `scripts/crawl_daiso.py`의 JS 스냅샷 로직 수정
- 확인: 재수집 후 1~10위 전부 `noimage.png`가 아닌 실제 상품 이미지 주소를 갖고 있음을
  확인. 카카오(7.8)·다이소 모두 결국 "지연 로딩 때문에 실제 로드되지 않은 이미지를
  그대로 수집"하는 같은 유형의 버그였음 — 앞으로 크롤러에 이미지 수집 로직을 새로
  추가할 때는 항상 `data-src`/`data-original` 같은 지연 로딩 속성이 있는지부터 확인할 것

## 7.12 순위 집계 기준 시각 표시 (2026-08-12)

- 요청: 화면에 "언제 기준 순위인지"가 날짜만 나오고 시간은 안 보여서, 시:분까지 보이게
  해달라는 요청
- 조치: 세 크롤러(`crawl_kakao_gift.py`/`crawl_daiso.py`/`crawl_oliveyoung.py`) 모두
  각 상품 행에 `수집시각`(예: `2026-08-12 18:53`, 한국 표준시 KST 고정) 컬럼을 새로 추가.
  `build_dashboard_data.py`는 최신 CSV의 `수집시각` 값을 읽어 `latest.json`의 `date`
  필드로 내려주고(옛날 CSV처럼 이 컬럼이 없으면 기존처럼 파일명의 날짜만 사용), 화면은
  이 값을 그대로 "OO 기준"으로 표시하므로 `report.html`은 수정할 필요 없었음
- 참고: GitHub Actions 실행 서버는 UTC 시간대라 `datetime.now()`를 그냥 쓰면 한국시간과
  9시간 어긋남 — `timezone(timedelta(hours=9))`로 KST를 명시해서 고정

## 7.13 화면 정리 — 소개 영역 축소 + 아래로 이동, 이름 변경 (2026-08-12)

- 요청: 상단 "AI 인터뷰 요약" 히어로 영역이 화면을 너무 많이 차지한다는 피드백. 영역을
  간소화하고 "오늘의 판매순위"보다 아래로 옮겨달라는 요청. 대시보드 이름도 "헬스랭크"에서
  "건강식품 데일리 랭킹"으로 변경 요청
- 조치:
  - 기존 히어로(큰 제목 `h1` 56px + eyebrow 배지 + 4칸 KPI 카드 그리드)를 통째로 삭제하고,
    작은 제목(`h2` 22px)과 한 줄 설명만 있는 `.about` 섹션으로 축소
  - 섹션 순서를 "오늘의 판매순위"(`#preview`) → `.about` 순으로 재배치 (전에는 반대)
  - `<title>`, 상단 네비게이션 브랜드명, 푸터 문구의 "헬스랭크"를 전부
    "건강식품 데일리 랭킹"으로 교체
  - 더 이상 쓰이지 않는 `.hero`/`.eyebrow`/`.card-grid`/`.card` CSS 규칙 삭제

## 7.14 5단계: 월간 판매랭킹 리포트 (2026-08-12)

- 사용자 아이디어: "월말/월초에 버튼을 누르면 월간 리포트가 자동 생성"되는 기능. 서버 없이
  깃허브 페이지로만 도는 정적 사이트 구조라, 버튼 클릭 시 실제로 서버에서 뭔가를 "생성"하는
  것은 불가능하다고 판단 — 대신 **매일 자동으로 미리 계산해두고, 버튼은 이미 계산된 결과를
  즉시 불러와 보여주는** 방식으로 설계 (사용자에게 "둘 다 원한다 — 즉석 보기부터, 자동
  보관은 나중에" 확인받고 진행)

### 설계상 핵심 포인트

1. **즉석 보기 + 자동 보관이 사실상 한 번에 해결됨**: `scripts/build_monthly_report.py`가
   매일 자동 실행되며 `data/monthly/{YYYY-MM}.json`을 다시 계산한다. 이번 달 파일은 매일
   최신 상태로 갱신되지만, 지난 달 파일은 그 달이 끝나는 순간부터 더 이상 손대지 않으므로
   자연스럽게 "보관"된다 — 별도의 아카이빙 로직을 따로 만들 필요가 없었음
2. **상품 식별 정확도 개선**: 실제 데이터를 보면 "[8월 올영픽]", "[신혜선 PICK]"처럼
   프로모션 문구가 상품명 앞에 붙었다 빠졌다 해서, 상품명만으로 매칭하면 같은 상품이
   날짜마다 "다른 상품"으로 쪼개질 위험이 있었음. 링크에 들어있는 고유 상품번호를
   정규식으로 추출해 진짜 식별자로 사용 — 카카오 `/product/(\d+)`, 다이소
   `pdNo=([^&]+)`, 올리브영 `goodsNo=([^&]+)`. 링크가 없거나 패턴이 안 맞으면 상품명으로
   대체(fallback). 이름/브랜드/가격/이미지는 그 상품의 가장 최근 날짜 값을 사용
3. **"반짝 1위" 왜곡 방지**: 하루만 반짝 1위 하고 사라진 상품이 한 달 내내 꾸준했던
   상품을 평균 순위로 제치지 않도록, 그 달 수집일의 최소 30%(`MIN_APPEARANCE_RATIO`,
   최소 1일) 이상 순위권에 들었던 상품만 TOP 10 후보로 인정. 정렬은 평균 순위 오름차순,
   동률이면 노출일수(appearances)가 많은 쪽이 우선
4. **검증**: 실제 데이터가 아직 하루치뿐이라, 스크래치패드에 10일치 가짜 CSV(상품A=매일
   꾸준히 3~4위, 상품B=하루만 반짝 1위, 상품C=날마다 이름이 바뀌지만 링크 ID는 동일)를
   만들어 별도 실행해봄 — 상품C가 이름이 바뀌어도 올바르게 하나로 합쳐지고(평균 2.0위,
   10일 노출), 상품B는 임계값 미달로 정상 제외되는 것을 확인

### 데이터/파일

- `scripts/build_monthly_report.py`: `data/raw/*.csv`를 사이트별·월별로 묶어
  `data/monthly/{YYYY-MM}.json`(상품별 `avg_rank`·`best_rank`·`top1_days`·`appearances`·
  `delta`(그 달 첫 관측일 대비 마지막 관측일 순위 변동) 등)과 사용 가능한 달 목록
  `data/monthly/index.json`을 생성
- `.github/workflows/daily-crawl.yml`: `build_dashboard_data.py` 다음에
  `build_monthly_report.py` 실행 스텝 추가, `data/monthly`도 함께 커밋
- `report.html`: "오늘의 판매순위" 섹션과 `.about` 섹션 사이에 "월간 판매랭킹 리포트"
  섹션 추가 — 월 선택 드롭다운(`data/monthly/index.json`으로 채움, 기본값 최신 달) +
  "월간 리포트 보기" 버튼. 클릭하면 `data/monthly/{선택한 달}.json`을 fetch해 사이트별
  TOP 10을 기존 순위 카드 스타일 그대로(`renderMonthlyCard`) 렌더링하되, 순위 숫자 대신
  평균 순위·노출일수·1위 횟수를 함께 표시

### 알려진 한계 (문서화만 하고 지금 당장 고치진 않음)

- 다이소·올리브영 크롤러는 매일 1~10위까지만 수집하므로, 10~12위권을 넘나드는 상품은
  순위 밖으로 밀린 날의 기록이 아예 없다(결측이 아니라 애초에 관측 대상이 아님). 그래서
  이런 상품의 평균 순위는 실제보다 좋게 보일 수 있음. 카카오는 1~20위까지 수집해 이
  영향이 상대적으로 적음

## 7.15 폰트 교체 — Pretendard 적용 (2026-08-12)

- 요청: 전반적으로 폰트가 단조로워 보여서, 심플하면서 세련된 폰트로 바꿔달라는 요청. AI
  분석글 기능은 비용 문제로 접고 이 작업으로 이어감
- 원인: 기존 `--font-display`/`--font-text`가 `-apple-system`/`SF Pro`/`Segoe UI` 등
  시스템 폰트만 나열하고 있어, 맥이 아닌 윈도우에서는 결국 Segoe UI(영문)+맑은 고딕(한글)
  기본값으로 보임 — 이 조합이 밋밋한 인상의 원인
- 조치: 한글·영문 모두 깔끔하게 나오는 오픈소스 폰트 **Pretendard**(토스·네이버 등에서
  널리 쓰임)를 jsDelivr CDN(`pretendard@v1.3.9`의 variable 웹폰트)으로 불러와
  `--font-display`/`--font-text` 맨 앞에 추가. 로드에 실패하는 경우를 대비해 기존
  시스템 폰트 스택은 그대로 폴백으로 남겨둠
- 확인: CDN 주소 응답 200 확인, 로컬 웹서버로 `<link>` 태그가 정상 포함된 채 서빙되는지
  확인 완료

## 7.16 오늘의 카테고리별 현황 (2026-08-12)

- 요청: 3채널을 종합해서 데일리로 "카테고리별(멀티비타민·홍삼·눈건강·여성건강·남성건강·
  간건강 등, 애매한 건 기타로) 순위 취합"을 표나 그래프로 보여달라는 요청. "중복 취합되는
  오류가 없게" 해달라는 요구 포함
- AI 없이(비용 문제로 AI 분석글 기능은 이미 접기로 함) 규칙 기반으로 구현 — 상품명에
  포함된 키워드로 분류. 실제 오늘자 상품 30개를 먼저 살펴보니 사용자가 말한 6개 카테고리
  외에도 다이어트·유산균·오메가3·콜라겐·관절·피부미용·에너지·혈당관리·장건강·면역·비타민이
  실제로 많이 나와서 카테고리를 17개(+기타)로 확장 — 자세한 목록과 우선순위는
  `scripts/build_category_report.py`의 `CATEGORY_RULES` 참고
- **"중복 취합 오류 방지"의 구체적 구현**: 상품 하나는 `CATEGORY_RULES` 순서대로 검사해
  **첫 번째로 매칭되는 카테고리 딱 하나에만** 배정한다(예: "질유산균"은 "유산균"보다 먼저
  검사하는 "여성건강"에 걸려서 유산균에는 안 잡힘). 스크립트 끝에서 `assert 카테고리별
  합계 총합 == 원본 상품 수`를 검증해, 로직에 중복/누락 버그가 생기면 그 자리에서 바로
  실패하도록 함
- 검증: 오늘 실데이터로 실행해보니 30개 중 24개(80%)가 구체적 카테고리로 분류되고 6개만
  "기타"로 남음 — 합리적인 비율
- **화면 — dataviz 스킬 절차대로 진행**:
  - 처음엔 카카오(노랑)·다이소(빨강)·올리브영(초록) 브랜드 컬러를 그래프에도 그대로
    쓰려 했으나, 색맹 시뮬레이션 검증(Node 미설치라 `validate_palette.js`의 로직을
    Python으로 동일하게 포팅해 실행)에서 실패함 — 빨강·노랑·초록은 대표적인 적록색맹
    혼동 조합이라 어두운 톤으로 조정해도 구분이 거의 안 됨. 그래서 그래프에는 dataviz
    스킬의 검증된 기본 팔레트(파랑·주황·아쿠아, 3개 슬롯 모두 all-pairs 기준 통과)를
    쓰고, 채널명은 항상 범례에 텍스트로 같이 표시해 색만으로 구분하지 않게 함(브랜드
    컬러는 위쪽 채널 타이틀 배경에서만 계속 사용)
  - 형태: 카테고리별 가로 스택 막대(3채널 구성 비율 + 합계 개수) + 그 아래 동일 데이터의
    표(`카테고리 데이터 표` 요청을 그대로 만족, 접근성을 위한 표 뷰 역할도 겸함)
  - 마크 스펙: 2px 간격으로 세그먼트 분리, 트랙 컨테이너에 4px 라운드, 합계 숫자는 막대
    밖(오른쪽)에 항상 표시해 잘림 없음, 각 세그먼트에 `title`/`aria-label`로 "채널명: N개"
    호버·포커스 정보 제공

### 데이터/파일

- `scripts/build_category_report.py`: `data/latest.json`(오늘 3채널 1~10위)을 읽어
  `data/category_daily.json`(카테고리별 `kakao`/`daiso`/`oliveyoung`/`total` 개수 +
  예시 상품 몇 개)을 생성
- `.github/workflows/daily-crawl.yml`: `build_monthly_report.py` 다음에
  `build_category_report.py` 실행 스텝 추가, `data/category_daily.json`도 함께 커밋
- `report.html`: "오늘의 판매순위"와 "월간 판매랭킹 리포트" 사이에 "오늘의 카테고리별
  현황" 섹션 추가 (배경색은 위아래 섹션과 번갈아 보이도록 조정)

## 7.17 "기타" 반복 키워드 자동 서제스트 (2026-08-12)

- 문제 제기: 데일리로 순위가 바뀌면 `CATEGORY_RULES`에 없는 새 유형의 상품이 계속
  "기타"로 쌓일 텐데, 그럴 때 오류가 나지 않는지, 자동으로 새 카테고리를 만들어주는
  기능이 있으면 좋겠다는 요청
- 먼저 확인해준 것: 지금 구조는 애초에 매칭 실패 시 예외 없이 "기타"로 떨어지도록
  설계돼 있어 크래시 위험은 없음(`classify()`의 기본 반환값)
- "완전 자동으로 새 카테고리를 발명"하는 건 사실상 의미 이해가 필요한 AI 영역이라
  (앞서 비용 때문에 접은 AI 분석글 기능과 같은 종류) 사용자에게 3가지 대안을 제시하고,
  **무료로 가능한 "기타 반복 키워드 자동 서제스트"**를 선택받음 — 완전 자동 분류는
  아니고, "이 단어들이 반복되니 카테고리 추가를 검토해보라"는 힌트를 자동으로 뽑아
  보여주는 방식
- 구현 (`scripts/build_category_report.py`에 추가):
  - `LOOKBACK_DAYS = 14`(최근 2주치) 동안의 `data/raw/*.csv`를 사이트별로 훑어 "기타"로
    분류된 상품만 모음
  - 같은 상품이 여러 날 반복 노출돼 후보 키워드 빈도가 부풀려지지 않도록, 월간 리포트와
    동일한 방식(링크 속 고유 상품번호)으로 **상품 단위 중복 제거** 후 집계
  - 상품명에서 한글 2글자 이상 토큰만 추출(`정규식 [가-힣]{2,}`)하고, `KEYWORD_STOPWORDS`
    (증정·기획·에디션 같은 마케팅 문구, 일분·개월분 같은 기간 단위어, 젤리·캡슐·필름 같은
    제형 단어)를 제외
  - 서로 다른 상품 **3개 이상**(`MIN_KEYWORD_COUNT`)에서 반복된 단어만, 빈도 높은 순
    최대 8개(`MAX_SUGGESTIONS`)까지 `data/category_daily.json`의 `keyword_suggestions`
    필드에 저장(단어 + 등장 횟수 + 예시 상품명 3개)
  - **검증**: 실데이터에 없는 가짜 테스트 채널로 "숙면 멜라토닌 젤리" 같은 상품을 10일치
    시뮬레이션해 실행 — "숙면"·"멜라토닌"이 정확히 후보로 잡히고, "일분"(기간 단위)과
    "젤리"(제형)는 스톱워드로 정상 제외됨을 확인. 테스트 파일은 실행 후 삭제
- 화면: 카테고리 표 아래에 후보가 있을 때만 나타나는 안내 박스 추가("기타"에 자주
  등장하는 단어 + 등장 횟수 칩, 칩에 마우스를 올리면 예시 상품명 표시). 오늘은 하루치
  데이터뿐이라 임계값(3개 이상 상품 반복)을 못 넘겨 후보가 0개 — 날짜가 쌓이면서 진짜
  신호가 나오기 시작할 것

## 7.18 6단계(팀/사내 공유용 확장) 로드맵에서 제외 (2026-08-12)

- 요청: "아직 이직 전이라서 6단계 팀원 및 내부공유 단계는 없는 거로 해줘"
- 배경: 인터뷰 당시(1절) "열람 대상"에 "우선 본인만 → 추후 팀/사내 공유로 확장"이라고
  답했었고, 그 "추후"가 이직을 전제로 한 계획이었음. 아직 이직 전이라 실제로 팀에
  공유할 대상 자체가 없는 상태
- 조치: 3절 로드맵에서 6단계("팀 공유용으로 배포")를 제거하고, 5단계(월간 리포트
  자동화)까지를 이 프로젝트의 현재 목표 범위로 정리. 7.1절에 있던 "6단계에서 공개
  여부 재검토" 계획도 더 이상 예정된 일정이 아님을 반영(지금은 공개 저장소 상태를
  그대로 유지 — 판매순위 데이터는 민감정보가 아니라 문제 없음)
- **되돌릴 수 있음**: 나중에 이직 등으로 상황이 바뀌면 이 결정은 언제든 다시 논의해서
  6단계를 로드맵에 되살릴 수 있다. 코드/기능을 삭제한 게 아니라 로드맵 계획만 뺀 것

## 7.19 카테고리별 현황 막대그래프 정렬 (2026-08-12)

- 요청: "오늘의 카테고리별 현황에서 막대그래프를 집계된 데이터 기준으로 내림차순 정렬"
- 문제: `scripts/build_category_report.py`가 카테고리를 고정된 `CATEGORY_ORDER` 순서
  그대로 `data/category_daily.json`에 저장하고 있었음 — report.html의
  `renderCategoryChart`/`renderCategoryTable`는 받은 배열 순서를 그대로 그리므로,
  화면의 막대그래프·표도 늘 같은 고정 순서로 나오고 있었음(상품 수와 무관)
- 조치: `categories` 리스트를 만든 직후 `total`(합계) 기준 내림차순으로 정렬하는 한 줄
  추가(`categories.sort(key=lambda c: c["total"], reverse=True)`). 스크립트를 재실행해
  `data/category_daily.json`을 다시 생성 — 오늘 기준 멀티비타민(8) → 기타(6) →
  다이어트(4) → … 순으로 정렬 확인함
- 매일 자동 실행되는 `daily-crawl.yml`이 이 스크립트를 그대로 다시 부르므로, 추가
  워크플로우 변경 없이 다음 자동 수집부터 항상 내림차순으로 나옴

## 7.20 카테고리별 현황 그래프 색상을 채널 브랜드 컬러로 통일 (2026-08-12)

- 요청: "그래프 색상을 전에 지정한 채널 키컬러로 수정해줘 그게 더 직관적으로 보여"
- 문제: 카테고리별 현황 막대그래프/범례가 범용 색상(`--cat-1` 파랑, `--cat-2` 주황,
  `--cat-3` 초록)을 쓰고 있어서, 7.9절에서 채널별 타이틀에 적용한 브랜드 키컬러
  (카카오 노랑 `#FEE500`, 다이소 빨강 `#ED1C24`, 올리브영 연두 `#9BCE26`)와 서로
  달라 같은 채널인데도 색이 이어지지 않는 문제가 있었음
- 조치: `--cat-1/2/3` 변수를 지우고 `--cat-kakao`/`--cat-daiso`/`--cat-oliveyoung`
  변수를 채널 타이틀과 동일한 브랜드 색상으로 정의(라이트/다크 모드 구분 없이 고정값 —
  채널 타이틀도 원래 다크모드에서 색이 안 바뀌므로 일관됨). JS의
  `CHANNEL_COLOR_VAR` 매핑도 새 변수명으로 갱신

## 7.21 자동 실행이 1시간 30분 지나도 안 되는 문제 — 깃허브 예약 실행 지연 (2026-08-13)

- 증상: 2026-08-13, 기준시간(09:30 KST)에서 1시간 30분이 지나도록 오늘자 랭킹이
  갱신되지 않음
- 점검(전부 정상으로 확인): `daily-crawl.yml`의 `cron: "30 0 * * *"` 설정 정상,
  워크플로우 상태 `active`, 저장소 공개(public), Actions 기능 켜짐 → 설정 오류는
  아니었음
- 원인: 실행 기록을 조회해보니 `schedule` 이벤트로 자동 발동된 실행이 지금까지
  단 한 건도 없었음(이전 실행 2건은 전부 수동 실행). 오늘이 사실상 이 예약이 처음
  발동해야 했던 날이었는데, 깃허브 공식 문서에 명시된 대로 "정각·30분처럼 다른
  저장소들도 몰리는 시각"에는 예약 실행이 크게 지연될 수 있음 — `00:30 UTC`(=09:30
  KST)가 바로 그런 혼잡 시각이라 지연된 것으로 보임
- 조치: ① 급한 오늘자 데이터는 `gh workflow run`으로 수동 실행시켜 즉시 확보
  ② 재발 방지를 위해 예약 시각을 혼잡을 피한 애매한 시각인 `37 0 * * *`
  (00:37 UTC = **09:37 KST**)로 변경. README.md의 "매일 09:30 KST" 표기도
  09:37로 함께 수정
- 참고: 매일 실행되는 자동화라 앞으로 며칠은 09:37 근처에 실제로 잘 도는지
  가끔 확인해볼 것 (`gh run list --repo cptmqhdl/healthrank --workflow daily-crawl.yml`)

## 7.22 크롤러 2건 CI 전용 타임아웃/레이스 컨디션 수정 (2026-08-13)

- 배경: 7.21절 지연 문제를 진단하던 중 수동 실행 로그에서 다이소몰 크롤러가
  타임아웃으로 실패한 걸 발견해 조사·수정 요청을 받음
- **다이소몰**(`crawl_daiso.py`): "건강식품 카테고리별 랭킹" 위젯까지
  스크롤하는 `scroll_into_view_if_needed`가 10초 타임아웃으로 실패. 로컬(윈도우
  PC)에서는 즉시 재현 없이 정상 수집돼 사이트 구조 변경이 아니라, 깃허브 액션
  실행 서버(해외 리전)에서 국내 사이트 접속이 로컬보다 느린 게 원인으로 추정.
  타임아웃을 10초→30초로 늘리고, 그래도 실패하면 3초 대기 후 1회 재시도하도록 수정
- **올리브영**(`crawl_oliveyoung.py`): 다이소 수정 후 재실행에서 이번엔 올리브영이
  `Execution context was destroyed, most likely because of a navigation` 오류로
  실패. `main.do` 방문 직후 fetch로 목록을 가져오는 `page.evaluate` 도중 페이지가
  다시 이동(리다이렉트/새로고침)하며 실행 컨텍스트가 끊긴 것으로 추정 — 로컬에서는
  재현되지 않아 CI의 느린 네트워크에서 더 잘 걸리는 레이스 컨디션으로 판단.
  `main.do` 방문 후 `networkidle` 대기(최대 10초, 실패해도 무시) + 정지 대기를
  1→2초로 늘려 페이지가 안정된 뒤에 목록을 가져오도록 하고, 그래도 실패하면
  3초 대기 후 1회(재방문부터) 재시도하도록 수정
- 검증: 두 크롤러 모두 로컬 실행 확인 후, `gh workflow run`으로 실제 깃허브
  액션에서 재실행해 검증. 다이소 수정 반영 실행에서는 다이소몰이 통과했고
  (해당 실행에서 올리브영이 별개로 실패해 이 문제를 발견), 올리브영 수정까지
  반영한 다음 실행(`31661456179`)에서 세 사이트 전부 통과 확인
- 교훈: 두 사이트 모두 **로컬에서는 항상 정상**이라 사이트 구조 문제와
  CI 특유의 네트워크 지연 문제를 구분하는 게 중요했음 — 앞으로 크롤러가 CI에서만
  실패하면 먼저 로컬 재현을 시도해 구조 변경인지 타이밍 문제인지부터 가릴 것

## 7.23 예약 실행 재조정 후에도 계속 미발동 → "지금 업데이트" 버튼 추가 (2026-08-14)

- 증상: 7.21절에서 "혼잡 시각(00:30 UTC) 지연"으로 추정해 예약 시각을 00:37 UTC(09:37
  KST)로 옮겼는데, 다음날(08-14) 그 시각이 한참 지나도록 오늘자 랭킹이 다시 갱신되지
  않음
- 재점검: `gh api repos/cptmqhdl/healthrank/actions/runs`로 전체 실행 이력을 조회한 결과,
  워크플로우 생성일(08-12)부터 지금까지 **`schedule` 이벤트로 발동된 실행이 단 한 건도
  없음**(기록된 실행은 전부 `workflow_dispatch`, 즉 수동 실행) — 08-13, 08-14 이틀
  연속으로 예약이 아예 발동하지 않은 것이 확인되어, 7.21절의 "혼잡 시각 지연" 추정은
  틀렸거나 최소한 원인의 전부가 아니었던 것으로 판단. 저장소 설정(cron 표현식, 워크플로우
  `active` 상태, 저장소 공개 여부, Actions 활성화 여부) 자체는 모두 정상이라 코드/설정
  오류는 아니고, 깃허브의 예약 실행 기능 자체를 신뢰할 수 없다고 결론
- 조치: ① 급한 08-14 데이터는 `gh workflow run`으로 즉시 수동 실행해 확보(run
  31766367557) ② 예약 실행에 더 이상 의존하지 않고, **대시보드에서 버튼 한 번으로 언제든
  수동 갱신할 수 있는 기능**을 추가하기로 방향 전환
- 설계 고민: 정적 사이트(GitHub Pages, 서버 없음)에서 버튼 클릭만으로 깃허브 Actions를
  실행하려면 인증 토큰이 필요한데, 화면에 그대로 넣으면 사이트 방문자 누구나 토큰을
  훔쳐갈 수 있어 안전하지 않음. 세 가지 방식(① 무료 중계 서버에 토큰을 숨기는 방식 ② 버튼을
  누르면 깃허브 로그인 후 수동으로 한 번 더 눌러야 하는 방식 ③ 제한된 토큰을 화면
  코드에 그대로 노출하는 방식)을 사용자에게 제시했고, 안전성과 완전 자동(원클릭)을 모두
  만족하는 **① 무료 Cloudflare Worker 경유 방식**을 선택함
- 구현:
  - `cloudflare-worker/trigger-worker.js` — Cloudflare Worker 코드(문서/백업 겸용, 실제
    배포는 Cloudflare 대시보드 온라인 편집기에 붙여넣어야 함). `GITHUB_TOKEN`을 Worker의
    암호화된 Secret으로만 보관하고, 요청이 오면 깃허브 API
    (`POST /repos/cptmqhdl/healthrank/actions/workflows/daily-crawl.yml/dispatches`)를
    대신 호출. `Access-Control-Allow-Origin`을 `https://cptmqhdl.github.io`로 고정해
    다른 출처의 요청은 CORS로 차단
  - 깃허브 토큰은 파인그레인드 토큰으로, healthrank 저장소 하나·Actions 읽기/쓰기 권한
    하나만 부여(그 외 전부 No access) — 유출되어도 피해 범위를 최소화
  - `report.html`의 "오늘의 판매순위" 섹션에 **"지금 업데이트" 버튼** 추가(기존
    `.monthly-btn` 스타일 재사용). 클릭 시 Worker 주소로 POST 요청 → 성공하면 "1~2분 안에
    끝나요, 새로고침 해보세요" 안내, 연타로 인한 중복 실행을 막기 위해 60초간 버튼
    재비활성화(서버 강제가 아닌 UX 가드)
- 남는 리스크: Worker 주소를 직접 알아내 호출하면 사이트를 거치지 않고도 크롤링을 반복
  실행시킬 수 있음(토큰 권한이 Actions:write뿐이라 저장소 코드·데이터 자체가 훼손될 위험은
  없고, 불필요한 실행 낭비 정도). 파인그레인드 토큰은 만료기간이 있어 만료 전 재발급 필요
  — 8절 열린 질문에 반영
- **후속(7.24절 참고): 이 Cloudflare Worker 방식은 실제로 배포되지 않고 폐기됨** — 사용자가
  깃허브 토큰 발급·Cloudflare 계정 생성 같은 설정 단계를 직접 하고 싶어하지 않아 더 간단한
  방식으로 대체함

## 7.24 설정 없이 되는 방식으로 최종 변경 (2026-08-14, 같은 날 이어짐)

- 7.23절에서 설계한 Cloudflare Worker 방식은 사용자가 직접 브라우저에서 처리해야 하는
  단계(깃허브 파인그레인드 토큰 발급, Cloudflare 무료 계정 생성)가 있었는데, 사용자가
  "니가 알아서 해줘"라며 이런 수동 설정을 원하지 않는다는 점을 분명히 함
- 시도 1: Claude Code의 클라우드 예약 작업(routine) 기능으로 매일 자동 트리거를 대신
  실행해주는 방법을 시도 — 이 경우 사용자가 새 계정/토큰을 만들 필요가 전혀 없음(gh
  CLI 인증은 클라우드 실행 환경에 이미 연결됨). 다만 이 기능도 **claude.ai 계정에
  깃허브 저장소 접근 권한을 한 번 연결**해야 하는 최소 조건이 있어(설정이라기보다
  깃허브 로그인 화면에서 "Authorize" 한 번 누르는 정도), 사용자에게 안내한 상태 —
  연결되면 매일 자동 실행하는 예약 작업을 마저 만들 예정
- 시도 2(즉시 반영): 위 클라우드 예약 작업 연결 전이라도 당장 쓸 수 있도록, "지금
  업데이트" 버튼을 **아무 설정도 필요 없는 방식**으로 단순화함 — 버튼을 누르면 깃허브의
  해당 워크플로우 실행 화면(`https://github.com/cptmqhdl/healthrank/actions/workflows/
  daily-crawl.yml`)을 새 탭으로 열어주기만 하고, 거기서 "Run workflow" 버튼을 한 번 더
  누르면 됨(이미 깃허브에 로그인돼 있으므로 추가 계정/토큰 불필요). 완전 원클릭은
  아니지만 설정 부담이 0이라는 게 장점
- 정리: `cloudflare-worker/trigger-worker.js`는 실제로 쓰이지 않게 되어 저장소에서 삭제.
  `report.html`의 버튼 클릭 핸들러도 Worker에 POST하던 코드에서 `window.open()`으로 깃허브
  실행 화면을 여는 코드로 교체

## 7.25 다이소 크롤러 반복 실패 근본 수정 + "지금 업데이트" 버튼 제거 (2026-08-18)

- 증상: 사용자가 대시보드를 확인하다가 카카오·올리브영은 오늘(8/18) 날짜로 갱신됐는데
  **다이소몰만 어제(8/17) 데이터에 멈춰있는 것**을 발견해 문의
- 조사: `gh run list`/`gh run view --log-failed`로 최근 실행 이력을 확인한 결과, 오늘뿐
  아니라 8/15, 8/16에도 다이소 크롤러가 정확히 같은 오류
  (`Locator.scroll_into_view_if_needed: Timeout 30000ms exceeded`, `crawl_daiso.py:48`)로
  실패해왔음을 확인 — 7.22절에서 "타임아웃 10→30초 + 1회 재시도"로 고쳤다고 봤던 문제가
  실제로는 재발하고 있었음(재시도해도 같은 방식이라 똑같이 실패)
- 원인 재분석: `build_dashboard_data.py`는 크롤러가 실패해도 `data/raw/`에서 **가장 최근
  파일**을 그냥 사용하도록 설계돼 있어(수집 첫날 대비용), 다이소 크롤러가 실패해도
  화면에는 에러 없이 어제 데이터가 계속 보였음 — 실패가 있어도 눈에 안 띄는 구조였던 게
  이번에 늦게 발견된 이유
- 근본 원인: `scroll_into_view_if_needed`는 Playwright가 요소가 "화면에 보이고 + 움직임이
  멈춰 안정된 상태"까지 확인한 뒤에만 성공 처리하는데, 다이소 페이지 위쪽에 자동으로
  움직이는 배너가 있어 레이아웃이 계속 흔들리면 이 "안정화" 조건이 영영 충족되지 않아
  타임아웃 나는 것으로 보임(로컬에서는 재현 안 되고 CI에서만 실패해온 것과도 일치 —
  CI가 느려 배너 애니메이션과 겹치는 시간이 더 길어짐)
- 조치: `widget.scroll_into_view_if_needed()` 대신 `widget.evaluate("el =>
  el.scrollIntoView({block:'center'})")`로 교체 — 브라우저 네이티브 스크롤을 직접 호출해
  Playwright의 안정성 대기 자체를 건너뜀. 재시도도 1회→3회로 늘림
- 검증: 로컬 재실행으로 오늘자(8/18) 다이소 데이터 10개 정상 수집 확인 후, 밀려있던
  집계(`data/latest.json`, `data/monthly/2026-08.json`, `data/category_daily.json`)를
  재생성해 반영. 실제 CI에서의 재발 여부는 내일(8/19) 09:37 예약 실행 결과로 확인 필요
- 동시 조치: 예약 실행(cron) 자체는 `gh run list` 확인 결과 8/14~8/18 5일 연속
  `schedule` 이벤트로 정상 발동하고 있어(7.21·7.23절 당시의 "예약이 아예 안 도는" 문제는
  해소된 것으로 판단), 그 시절 임시방편으로 넣었던 **"지금 업데이트" 버튼을 제거**함
  (관련 HTML/CSS/JS 전부 삭제). 예약이 다시 불안정해지면 재도입 검토
- 후속 확인 필요: 내일 이후 며칠간 다이소 데이터가 매일 잘 갱신되는지
  (`gh run list --repo cptmqhdl/healthrank --workflow daily-crawl.yml`로) 가끔 확인할 것 —
  같은 타임아웃이 또 발생하면 배너 애니메이션이 원인이라는 추정이 맞는지 재검토 필요

## 8. 열린 질문 / 리스크

- 사이트 구조가 바뀌면 유지보수 필요 — 선택자/카테고리 코드가 깨질 수 있어 주기적 점검 필요
- 자동화가 매일 도니, 사이트에서 접속을 차단할 가능성을 계속 지켜봐야 함 (막히면 크롤러가
  SystemExit(1)로 실패해서 Actions 탭에 표시됨)
- 저장소는 현재 공개 상태(2026-08-12부터). 6단계(팀 공유)가 로드맵에서 빠지면서
  공개 여부를 다시 검토할 예정된 시점은 없어졌지만, 이직 등으로 팀 공유가 다시
  필요해지면 그때 접근 권한을 재검토할 것
- 자동 접속이 반복되면 사이트에서 차단할 가능성이 있으므로, 매일 실행 간격을 적당히 두고
  과도한 요청은 피할 것
- 깃허브 예약(cron) 자동 실행은 7.21·7.23절 당시 불안정했으나, 8/14~8/18 5일 연속
  정상 발동을 확인해(7.25절) 안정화된 것으로 보고 "지금 업데이트" 버튼은 제거함. 다만
  다시 며칠간 안 도는 일이 발생하면 재검토 필요 — 가끔 `gh run list --repo
  cptmqhdl/healthrank`로 확인해볼 것
- 다이소 크롤러의 `scroll_into_view_if_needed` → 네이티브 `scrollIntoView` 교체(7.25절)가
  실제 CI 환경에서도 재발을 막는지는 아직 1회 검증(로컬)뿐 — 며칠 더 지켜볼 것

---
*이 문서는 진행 상황에 맞춰 계속 업데이트합니다.*
